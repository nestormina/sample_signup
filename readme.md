You'll need MongoDB installed and running
Remember to change DB name if necessary:
- backend/server.ts:17

To run the project:

cd backend
npm install
npm run dev

cd frontend
npm install
npm run dev
